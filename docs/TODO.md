# 未完成事项清单

> 汇总自各 dev-log 的遗留项 + 路线图剩余工作 + 必须真机/真实环境才能做的验证。
> 更新时间：2026-06-12。完成一项划掉一项；提交前同步到 design.md「未完成说明」。

## 一、待开发（按优先级）

### 比赛交付物（必做）

- [ ] **PR #14：README 定稿**——依赖声明表（含许可证）、安装运行方式、
  使用说明（语音指令示例、设置面板配 Key）、能力清单链接
- [ ] **PR #15：demo 视频录制**（建议流程：基础绘制 → 复合指令 →
  "画一个房子，左边加一棵树，右上角有太阳" → 改色/移动/缩放 → 撤销/回放 →
  接入真实 LLM 演示"画一个人"+ 追问闭环）
- [ ] design.md「未完成说明」终稿填写（提交前以本文档为准誊写）

### 功能增强（视时间取舍）

- [ ] 高级指代消解（能力 #15a）："左边那个"/"第二个"/"最大的那个"
- [ ] relativeTo 扩展：between（"在圆和三角之间"）、near、inside
- [ ] 自由笔刷（能力 #19）："开始画线…往右…往下…停"
- [ ] 组合图形"组"概念：模板产物目前是多个独立对象，"把太阳移到左边"
  需要逐个对象操作；可引入 group id 让模板整体选中/移动/删除
- [ ] 文字缩放：fontSize 变化需同步重算居中偏移，目前 resize 对 text 报"不支持"
- [ ] 追问轮数上限代码级硬限制（目前仅提示词约束，极端情况 LLM 可能无限追问）

### 已知小缺陷（不阻塞演示）

- [ ] 复合指令对同一对象连续两次移动：第二次读到动画中间坐标，终点有偏差
  （规则引擎不会产出此组合，仅 LLM 理论可能；修法：坐标读取也走 pendingAttrs）
- [ ] 直线缩放后 strokeWidth 随 scale 变粗（可在缩放时反向补偿 strokeWidth）
- [ ] 模板线条在非 3:2 画布上轻微变形（fx 偏移按 3:2 固定折减 0.67）
- [ ] LLM 复合指令混入 undo/redo 时历史栈顺序不直观（不破坏状态，罕见路径）
- [ ] 连续多次移动各计一条撤销历史，未做合并（体验问题，非错误）
- [ ] TTS 中断时识别恢复依赖 utterance onend/onerror，个别浏览器可能不触发

## 二、待验证（需要真实环境/用户操作）

### 真实 AI（最高优先：目前 LLM 链路只有 mock 验证）

- [ ] **真实 Key 实测 LLM 拆解质量**：建议智谱 glm-4-flash（免费）先跑通，
  再试 DeepSeek。重点观察：
  - "画一个人/雪人以外的开放对象"拆解出的图形协调性（提示词可能需调优）
  - 追问触发是否恰当（不该问的别问、该问的要问，上限两轮是否被遵守）
  - JSON 模式输出稳定性（校验失败率）；各服务商 OpenAI 兼容差异
- [ ] 真实网络下慢路径延迟（设计预算 1-2s，超出需调整"思考中"提示节奏）

### 真机（无麦克风环境做不了，阻塞至今）

- [ ] **Edge 真机语音识别验证**（dev-log 02 遗留）：中文流式识别连续 10 分钟，
  覆盖断句、静音自动重连、权限弹窗；失败则启动方案 B（七牛云 ASR，
  目前只有设计无代码，见 design.md §6.1）
- [ ] **TTS 真机听感**：音色/语速（rate=1.1）调整；自听回环防护在真麦克风下实测
  （播报期间暂停识别→恢复的时序是否漏字）
- [ ] 语音输入下的端到端延迟实测（目标：简单指令 < 1.5s，design.md §5）

### 工程收尾

- [ ] **按序合并 PR #5 → #13**（用户在 GitHub 网页操作；⚠️ 远端 main 目前只有
  初始文档提交，合并前别在 main 上测试）
- [ ] 合并后在 main 上完整跑一遍回归：`npx tsx scripts/parser-smoke.ts` +
  全部 `node scripts/e2e-*.mjs`（前置 `npm run dev`）
- [ ] 多分辨率/缩放下画布表现抽查（坐标全是比例换算，理论无碍，未实测）
- [ ] 大量对象（50+）下快照/撤销/回放性能压测（快照为全量序列化，
  上限 50 条历史，理论可控，未实测）
