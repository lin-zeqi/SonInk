# 未完成事项清单

> 汇总自各 dev-log 的遗留项 + 路线图剩余工作 + 必须真机/真实环境才能做的验证。
> 更新时间：2026-06-14。完成一项划掉一项；提交前同步到 design.md「未完成说明」。

## 一、待开发（按优先级）

### 比赛交付物（必做）

- [x] **PR #14：README 定稿**——依赖声明表（含许可证）、安装运行方式、
  使用说明（语音指令示例、设置面板配 Key）、能力清单链接
- [x] **PR #15：demo 视频录制**——分镜脚本已完成（`docs/demo-script.md`），
  实际录制需真人执行
- [x] design.md「未完成说明」终稿填写（提交前以本文档为准誊写）

### 功能增强（视时间取舍）

- [ ] 高级指代消解（能力 #15a）："左边那个"/"第二个"/"最大的那个"
- [ ] relativeTo 扩展：between（"在圆和三角之间"）、near、inside
- [ ] 自由笔刷（能力 #19）："开始画线…往右…往下…停"
- [x] ~~组合图形"组"概念~~ — 已通过 groupName/groupId/part 实现（feat/13-canvas-power 工作区），模板改为单 path，LLM 使用多 path + 共享 groupName
- [ ] 文字缩放：fontSize 变化需同步重算居中偏移，目前 resize 对 text 报"不支持"
- [ ] 追问轮数上限代码级硬限制（目前仅提示词约束，极端情况 LLM 可能无限追问）

### 已知小缺陷（不阻塞演示）

- [ ] 复合指令对同一对象连续两次移动：第二次读到动画中间坐标，终点有偏差
- [ ] 直线缩放后 strokeWidth 随 scale 变粗
- [ ] 模板线条在非 3:2 画布上轻微变形（fx 偏移按 3:2 固定折减 0.67）
- [ ] LLM 复合指令混入 undo/redo 时历史栈顺序不直观（不破坏状态，罕见路径）
- [ ] 连续多次移动各计一条撤销历史，未做合并
- [ ] TTS 中断时识别恢复依赖 utterance onend/onerror，个别浏览器可能不触发

## 二、E2E 测试（全部通过 ✅）

全部 8 个 e2e 脚本已在本地 Edge 上跑通：

| 脚本 | 覆盖 PR | 验证点 |
|---|---|---|
| `e2e-anim.mjs` | #7 | 渐进绘制动画 |
| `e2e-repro.mjs` | #6 | 选中/移动/删除/连接词拆分 |
| `e2e-llm.mjs` | #8 | LLM 通道（mock） |
| `e2e-undo.mjs` | #9 | 撤销/重做事务快照 |
| `e2e-resize-move.mjs` | #10 | 缩放+移动含过渡动画 |
| `e2e-tts.mjs` | #11 | TTS 反馈+清空确认 |
| `e2e-robust.mjs` | #12 | 相对定位/缺图形追问/口语动词 |
| `e2e-canvas-power.mjs` | #13 | 语义模板/文本/改色/导出/回放/拖拽 |

### 真机语音（已在 Edge + 麦克风环境验证 ✅）

- [x] Edge 中文流式识别连续运行，断句、静音自动重连、权限弹窗均正常
- [x] TTS 播报听感可接受（rate=1.1），自听回环防护正常工作（播报期间暂停识别→恢复时序正确）
- [x] 简单指令端到端延迟在 1.5s 目标内

### 真实 AI（已实测 ✅）

- [x] DeepSeek API 真实 Key 实测通过，JSON 模式输出稳定，校验失败率低
- [x] 追问触发恰当，两轮上限被遵守
- [x] 慢路径延迟在设计预算 1-2s 内

### 工程收尾

- [ ] **PR 合并**：feat/13-canvas-power → main（当前分支含未提交工作区变更）
- [ ] 合并后在 main 上完整回归：`npx tsx scripts/parser-smoke.ts` + 全部 `node scripts/e2e-*.mjs`
- [ ] 多分辨率/缩放下画布表现抽查
- [ ] 大量对象（50+）下快照/撤销/回放性能压测
